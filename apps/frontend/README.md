# StudyKit

## HTML

В Vite единственная точка входа — `index.html` в корне приложения. Папка `public/` содержит только статику (favicon, manifest). Файл `public/index.html` был артефактом Create React App и удалён.
